import React from 'react'

const MovieTrailerPlayer = ({ trailerUrl }) => (
    <div>
        <a href={trailerUrl}><span>Link to play trailer</span></a>
    </div>
)

export default MovieTrailerPlayer
